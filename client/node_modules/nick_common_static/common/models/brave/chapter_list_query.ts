import qb = require('common/models/common/query_base');

export module Brave {
    /*
     * 章节列表搜索类
     * */
    export class ChapterListQuery extends qb.Common.QueryBase {
        public tower:string;

        constructor(data?:any) {
            super(data);
        }
    }
}