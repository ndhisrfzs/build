import qb = require('common/models/common/query_base');

export module Brave {
    /*
     * 问题列表搜索类
     * */
    export class StageListQuery extends qb.Common.QueryBase {
        public series:string;
        public chapter:number;
        public tower:string;

        constructor(data?:any) {
            super(data);
        }
    }
}